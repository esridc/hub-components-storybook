import { Format } from 'ajv';
/**
 * TODO: if we need to pass context into the formatters, let's make this into a function
 * so that we can give context as a param
 */
export declare const AJV_FORMATS: Record<string, Format>;
