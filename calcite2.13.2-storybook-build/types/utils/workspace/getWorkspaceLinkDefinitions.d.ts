import { WorkspaceLinkDefinition } from './types';
import { HubEntityType } from '@esri/hub-common';
/**
 * returns the workspace link definitions based on the entity type
 */
export declare function getWorkspaceLinkDefinitions(entityType: HubEntityType): WorkspaceLinkDefinition[];
